<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>GitPub</title>
    <style>
        /* Your existing styles */
    </style>
</head>

<body>
    <h1>Welcome to GitPub</h1>
    <ul>
        <% drinks.forEach(function(drink) { %>
            <li><%= capitalizeFirstLetter(drink.name) %></li>
        <% }); %>
    </ul>

    <!-- Script to capitalize the first letter of a string -->
    <script>
        function capitalizeFirstLetter(string) {
            return string.charAt(0).toUpperCase() + string.slice(1);
        }
    </script>
</body>

</html>
